const express = require('express');

const router = express.Router();

router.get('/:userId/totalRecord', (req, res, next) => {});
router.get('/:userId/recentRecord', (req, res, next) => {});

module.exports = router;
